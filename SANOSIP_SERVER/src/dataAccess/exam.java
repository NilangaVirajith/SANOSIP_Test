/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package dataAccess;

import java.sql.Connection;
import java.sql.SQLException;
import utilities.DB;

/**
 *
 * @author Hishara
 */
public class exam {
//    public int registerExam(model.Exam newExam) throws ClassNotFoundException, SQLException {
//        Connection con = DB.getConnection();
//        String query = "insert into exam values('NULL','" + newExam.getClassID() + "','" + newExam.getDate() + "')";
//        int spd = utilities.DB_handler.setData(con, query);
//        return spd;
//    }
}
